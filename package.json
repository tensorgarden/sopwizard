{
  "name": "sopwizard",
  "version": "0.9.0",
  "private": true,
  "description": "Record a browser workflow and generate a clean, structured SOP.",
  "scripts": {
    "pack:extension": "rm -rf dist && mkdir -p dist && zip -qr dist/sopwizard-extension.zip extension && echo built dist/sopwizard-extension.zip"
  },
  "workspaces": [
    "server"
  ],
  "engines": {
    "node": ">=20"
  }
}
